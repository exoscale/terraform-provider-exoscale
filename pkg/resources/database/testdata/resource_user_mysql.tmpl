resource "exoscale_database_mysql_user" {{ .ResourceName }} {
  username = "{{ .Username }}"
  service = {{ .Service }}
  zone = "{{ .Zone }}"

  {{- if .Authentication }}
  authentication = "{{ .Authentication }}"
  {{- end }}
}
