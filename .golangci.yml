run:
  timeout: 10m

linters-settings:
  revive:
    rules:
      - name: if-return
        disabled: true

linters:
  enable:
    - deadcode
    - errcheck
    - exportloopref
    - gocritic
    - goimports
    - gosimple
    - govet
    - ineffassign
    - megacheck
    - revive
    - staticcheck
    - structcheck
    - unused
    - varcheck
  disable-all: true
