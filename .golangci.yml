run:
  timeout: 10m

linters-settings:
  revive:
    rules:
      - name: if-return
        disabled: true

linters:
  enable:
    - errcheck
    - exportloopref
    - gocritic
    - goimports
    - gosimple
    - govet
    - ineffassign
    - megacheck
    - revive
    - staticcheck
    - unused
  disable-all: true
